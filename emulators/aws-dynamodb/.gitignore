# Python
__pycache__/
*.pyc
.venv/

# DynamoDB Local (downloaded by install.sh, not committed)
dynamodb-local/
dynamodb-local-metadata.json

# Test artifacts
tests/eval_results.json
tests/test.sh

# AWS credentials / config written by install.sh (local only)
# ~/.aws is outside the repo, but guard against accidental copies
*.credentials

# Misc
.DS_Store
